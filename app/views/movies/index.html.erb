<h1 class="text-center mt-4">Movies listing</h1>

<div class="flickr_pagination">
  <%= will_paginate @movies, :container => false %>
</div>

<%= render 'movie' %>

<div class="flickr_pagination mb-4">
  <%= will_paginate @movies, :container => false %>
</div>